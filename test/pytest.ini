[pytest]
addopts = --driver=chrome --disable-warnings -n 0
filterwarnings =
    ignore::UserWarning:sensitiveurl
log_cli = true
selenium_driver = Chrome
selenium_driver_args = --headless --disable-gpu
sensitive_url_filter = test-le.sia-service.kr
markers =
    sensitive_url: mark test as sensitive_url
